import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.houseplant.analyzer',
  appName: 'Houseplant Analyzer',
  webDir: 'out'
};

export default config;
